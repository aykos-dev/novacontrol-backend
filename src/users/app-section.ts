export enum AppSection {
  DASHBOARD = 'dashboard',
  CLIENTS = 'clients',
  FINANCE = 'finance',
  ANALYTICS = 'analytics',
  EXPENSE_CATEGORIES = 'expenseCategories',
  USERS = 'users',
  SETTINGS = 'settings',
}

export const ALL_APP_SECTIONS = Object.values(AppSection);
